import request from "supertest";
import { app } from "../../../index";

describe("create note", () => {
  it("simply creates a note", () => {
    // request(app)
    //   .get("/notes")
    //   .set("Accept", "application/json")
    //   .expect("Content-Type", /json/)
    //   .expect(200);
    expect(true).toBeTruthy();
  });
});
